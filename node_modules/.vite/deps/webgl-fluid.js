import "./chunk-G3PMV62Z.js";

// node_modules/webgl-fluid/dist/webgl-fluid.mjs
function e(e2, t) {
  let n = e2;
  ke(), t = {
    IMMEDIATE: true,
    TRIGGER: "hover",
    AUTO: false,
    INTERVAL: 3e3,
    SIM_RESOLUTION: 128,
    DYE_RESOLUTION: 1024,
    CAPTURE_RESOLUTION: 512,
    DENSITY_DISSIPATION: 1,
    VELOCITY_DISSIPATION: 0.2,
    PRESSURE: 0.8,
    PRESSURE_ITERATIONS: 20,
    CURL: 30,
    SPLAT_RADIUS: 0.25,
    SPLAT_FORCE: 6e3,
    SPLAT_COUNT: Number.parseInt(Math.random() * 20) + 5,
    SPLAT_COLOR: void 0,
    SHADING: true,
    COLORFUL: true,
    COLOR_UPDATE_SPEED: 10,
    PAUSED: false,
    BACK_COLOR: {
      r: 0,
      g: 0,
      b: 0
    },
    TRANSPARENT: false,
    BLOOM: true,
    BLOOM_ITERATIONS: 8,
    BLOOM_RESOLUTION: 256,
    BLOOM_INTENSITY: 0.8,
    BLOOM_THRESHOLD: 0.6,
    BLOOM_SOFT_KNEE: 0.7,
    SUNRAYS: true,
    SUNRAYS_RESOLUTION: 196,
    SUNRAYS_WEIGHT: 1,
    ...t
  };
  function r() {
    this.id = -1, this.texcoordX = 0, this.texcoordY = 0, this.prevTexcoordX = 0, this.prevTexcoordY = 0, this.deltaX = 0, this.deltaY = 0, this.down = false, this.moved = false, this.color = Z();
  }
  let i = [], a = [], o = [];
  i.push(new r());
  let { gl: s, ext: c } = l(n);
  te() && (t.DYE_RESOLUTION = 512), c.supportLinearFiltering || (t.DYE_RESOLUTION = 512, t.SHADING = false, t.BLOOM = false, t.SUNRAYS = false);
  function l(e3) {
    let t2 = {
      alpha: true,
      depth: false,
      stencil: false,
      antialias: false,
      preserveDrawingBuffer: false
    }, n2 = e3.getContext("webgl2", t2), r2 = !!n2;
    r2 || (n2 = e3.getContext("webgl", t2) || e3.getContext("experimental-webgl", t2));
    let i2, a2;
    r2 ? (n2.getExtension("EXT_color_buffer_float"), a2 = n2.getExtension("OES_texture_float_linear")) : (i2 = n2.getExtension("OES_texture_half_float"), a2 = n2.getExtension("OES_texture_half_float_linear")), n2.clearColor(0, 0, 0, 1);
    let o2 = r2 ? n2.HALF_FLOAT : i2.HALF_FLOAT_OES, s2, c2, l2;
    return r2 ? (s2 = u(n2, n2.RGBA16F, n2.RGBA, o2), c2 = u(n2, n2.RG16F, n2.RG, o2), l2 = u(n2, n2.R16F, n2.RED, o2)) : (s2 = u(n2, n2.RGBA, n2.RGBA, o2), c2 = u(n2, n2.RGBA, n2.RGBA, o2), l2 = u(n2, n2.RGBA, n2.RGBA, o2)), {
      gl: n2,
      ext: {
        formatRGBA: s2,
        formatRG: c2,
        formatR: l2,
        halfFloatTexType: o2,
        supportLinearFiltering: a2
      }
    };
  }
  function u(e3, t2, n2, r2) {
    if (!ee(e3, t2, n2, r2)) switch (t2) {
      case e3.R16F:
        return u(e3, e3.RG16F, e3.RG, r2);
      case e3.RG16F:
        return u(e3, e3.RGBA16F, e3.RGBA, r2);
      default:
        return null;
    }
    return {
      internalFormat: t2,
      format: n2
    };
  }
  function ee(e3, t2, n2, r2) {
    let i2 = e3.createTexture();
    e3.bindTexture(e3.TEXTURE_2D, i2), e3.texParameteri(e3.TEXTURE_2D, e3.TEXTURE_MIN_FILTER, e3.NEAREST), e3.texParameteri(e3.TEXTURE_2D, e3.TEXTURE_MAG_FILTER, e3.NEAREST), e3.texParameteri(e3.TEXTURE_2D, e3.TEXTURE_WRAP_S, e3.CLAMP_TO_EDGE), e3.texParameteri(e3.TEXTURE_2D, e3.TEXTURE_WRAP_T, e3.CLAMP_TO_EDGE), e3.texImage2D(e3.TEXTURE_2D, 0, t2, 4, 4, 0, n2, r2, null);
    let a2 = e3.createFramebuffer();
    return e3.bindFramebuffer(e3.FRAMEBUFFER, a2), e3.framebufferTexture2D(e3.FRAMEBUFFER, e3.COLOR_ATTACHMENT0, e3.TEXTURE_2D, i2, 0), e3.checkFramebufferStatus(e3.FRAMEBUFFER) === e3.FRAMEBUFFER_COMPLETE;
  }
  function te() {
    return /Mobi|Android/i.test(navigator.userAgent);
  }
  class ne {
    constructor(e3, t2) {
      this.vertexShader = e3, this.fragmentShaderSource = t2, this.programs = [], this.activeProgram = null, this.uniforms = [];
    }
    setKeywords(e3) {
      let t2 = 0;
      for (let n3 = 0; n3 < e3.length; n3++) t2 += et(e3[n3]);
      let n2 = this.programs[t2];
      if (!n2) {
        let r2 = m(s.FRAGMENT_SHADER, this.fragmentShaderSource, e3);
        n2 = f(this.vertexShader, r2), this.programs[t2] = n2;
      }
      n2 !== this.activeProgram && (this.uniforms = p(n2), this.activeProgram = n2);
    }
    bind() {
      s.useProgram(this.activeProgram);
    }
  }
  class d {
    constructor(e3, t2) {
      this.uniforms = {}, this.program = f(e3, t2), this.uniforms = p(this.program);
    }
    bind() {
      s.useProgram(this.program);
    }
  }
  function f(e3, t2) {
    let n2 = s.createProgram();
    if (s.attachShader(n2, e3), s.attachShader(n2, t2), s.linkProgram(n2), !s.getProgramParameter(n2, s.LINK_STATUS)) throw s.getProgramInfoLog(n2);
    return n2;
  }
  function p(e3) {
    let t2 = [], n2 = s.getProgramParameter(e3, s.ACTIVE_UNIFORMS);
    for (let r2 = 0; r2 < n2; r2++) {
      let n3 = s.getActiveUniform(e3, r2).name;
      t2[n3] = s.getUniformLocation(e3, n3);
    }
    return t2;
  }
  function m(e3, t2, n2) {
    t2 = re(t2, n2);
    let r2 = s.createShader(e3);
    if (s.shaderSource(r2, t2), s.compileShader(r2), !s.getShaderParameter(r2, s.COMPILE_STATUS)) throw s.getShaderInfoLog(r2);
    return r2;
  }
  function re(e3, t2) {
    if (!t2) return e3;
    let n2 = "";
    return t2.forEach((e4) => {
      n2 += `#define ${e4}
`;
    }), n2 + e3;
  }
  let h = m(s.VERTEX_SHADER, "\n    precision highp float;\n    attribute vec2 aPosition;\n    varying vec2 vUv;\n    varying vec2 vL;\n    varying vec2 vR;\n    varying vec2 vT;\n    varying vec2 vB;\n    uniform vec2 texelSize;\n    void main () {\n        vUv = aPosition * 0.5 + 0.5;\n        vL = vUv - vec2(texelSize.x, 0.0);\n        vR = vUv + vec2(texelSize.x, 0.0);\n        vT = vUv + vec2(0.0, texelSize.y);\n        vB = vUv - vec2(0.0, texelSize.y);\n        gl_Position = vec4(aPosition, 0.0, 1.0);\n    }\n"), ie = m(s.VERTEX_SHADER, "\n    precision highp float;\n    attribute vec2 aPosition;\n    varying vec2 vUv;\n    varying vec2 vL;\n    varying vec2 vR;\n    uniform vec2 texelSize;\n    void main () {\n        vUv = aPosition * 0.5 + 0.5;\n        float offset = 1.33333333;\n        vL = vUv - texelSize * offset;\n        vR = vUv + texelSize * offset;\n        gl_Position = vec4(aPosition, 0.0, 1.0);\n    }\n"), ae = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    precision mediump sampler2D;\n    varying vec2 vUv;\n    varying vec2 vL;\n    varying vec2 vR;\n    uniform sampler2D uTexture;\n    void main () {\n        vec4 sum = texture2D(uTexture, vUv) * 0.29411764;\n        sum += texture2D(uTexture, vL) * 0.35294117;\n        sum += texture2D(uTexture, vR) * 0.35294117;\n        gl_FragColor = sum;\n    }\n"), oe = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    precision mediump sampler2D;\n    varying highp vec2 vUv;\n    uniform sampler2D uTexture;\n    void main () {\n        gl_FragColor = texture2D(uTexture, vUv);\n    }\n"), se = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    precision mediump sampler2D;\n    varying highp vec2 vUv;\n    uniform sampler2D uTexture;\n    uniform float value;\n    void main () {\n        gl_FragColor = value * texture2D(uTexture, vUv);\n    }\n"), ce = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    uniform vec4 color;\n    void main () {\n        gl_FragColor = color;\n    }\n"), le = m(s.FRAGMENT_SHADER, t.TRANSPARENT ? "\n    precision highp float;\n    precision highp sampler2D;\n    varying vec2 vUv;\n    uniform sampler2D uTexture;\n    uniform float aspectRatio;\n    #define SCALE 25.0\n    void main () {\n        vec2 uv = floor(vUv * SCALE * vec2(aspectRatio, 1.0));\n        float v = mod(uv.x + uv.y, 2.0);\n        v = v * 0.1 + 0.8;\n        gl_FragColor = vec4(0.0, 0.0, 0.0, 0.0);\n    }\n" : "\n    precision highp float;\n    precision highp sampler2D;\n    varying vec2 vUv;\n    uniform sampler2D uTexture;\n    uniform float aspectRatio;\n    #define SCALE 25.0\n    void main () {\n        vec2 uv = floor(vUv * SCALE * vec2(aspectRatio, 1.0));\n        float v = mod(uv.x + uv.y, 2.0);\n        v = v * 0.1 + 0.8;\n        gl_FragColor = vec4(vec3(v), 1.0);\n    }\n"), ue = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    precision mediump sampler2D;\n    varying vec2 vUv;\n    uniform sampler2D uTexture;\n    uniform vec3 curve;\n    uniform float threshold;\n    void main () {\n        vec3 c = texture2D(uTexture, vUv).rgb;\n        float br = max(c.r, max(c.g, c.b));\n        float rq = clamp(br - curve.x, 0.0, curve.y);\n        rq = curve.z * rq * rq;\n        c *= max(rq, br - threshold) / max(br, 0.0001);\n        gl_FragColor = vec4(c, 0.0);\n    }\n"), de = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    precision mediump sampler2D;\n    varying vec2 vL;\n    varying vec2 vR;\n    varying vec2 vT;\n    varying vec2 vB;\n    uniform sampler2D uTexture;\n    void main () {\n        vec4 sum = vec4(0.0);\n        sum += texture2D(uTexture, vL);\n        sum += texture2D(uTexture, vR);\n        sum += texture2D(uTexture, vT);\n        sum += texture2D(uTexture, vB);\n        sum *= 0.25;\n        gl_FragColor = sum;\n    }\n"), fe = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    precision mediump sampler2D;\n    varying vec2 vL;\n    varying vec2 vR;\n    varying vec2 vT;\n    varying vec2 vB;\n    uniform sampler2D uTexture;\n    uniform float intensity;\n    void main () {\n        vec4 sum = vec4(0.0);\n        sum += texture2D(uTexture, vL);\n        sum += texture2D(uTexture, vR);\n        sum += texture2D(uTexture, vT);\n        sum += texture2D(uTexture, vB);\n        sum *= 0.25;\n        gl_FragColor = sum * intensity;\n    }\n"), pe = m(s.FRAGMENT_SHADER, "\n    precision highp float;\n    precision highp sampler2D;\n    varying vec2 vUv;\n    uniform sampler2D uTexture;\n    void main () {\n        vec4 c = texture2D(uTexture, vUv);\n        float br = max(c.r, max(c.g, c.b));\n        c.a = 1.0 - min(max(br * 20.0, 0.0), 0.8);\n        gl_FragColor = c;\n    }\n"), me = m(s.FRAGMENT_SHADER, "\n    precision highp float;\n    precision highp sampler2D;\n    varying vec2 vUv;\n    uniform sampler2D uTexture;\n    uniform float weight;\n    #define ITERATIONS 16\n    void main () {\n        float Density = 0.3;\n        float Decay = 0.95;\n        float Exposure = 0.7;\n        vec2 coord = vUv;\n        vec2 dir = vUv - 0.5;\n        dir *= 1.0 / float(ITERATIONS) * Density;\n        float illuminationDecay = 1.0;\n        float color = texture2D(uTexture, vUv).a;\n        for (int i = 0; i < ITERATIONS; i++)\n        {\n            coord -= dir;\n            float col = texture2D(uTexture, coord).a;\n            color += col * illuminationDecay * weight;\n            illuminationDecay *= Decay;\n        }\n        gl_FragColor = vec4(color * Exposure, 0.0, 0.0, 1.0);\n    }\n"), he = m(s.FRAGMENT_SHADER, "\n    precision highp float;\n    precision highp sampler2D;\n    varying vec2 vUv;\n    uniform sampler2D uTarget;\n    uniform float aspectRatio;\n    uniform vec3 color;\n    uniform vec2 point;\n    uniform float radius;\n    void main () {\n        vec2 p = vUv - point.xy;\n        p.x *= aspectRatio;\n        vec3 splat = exp(-dot(p, p) / radius) * color;\n        vec3 base = texture2D(uTarget, vUv).xyz;\n        gl_FragColor = vec4(base + splat, 1.0);\n    }\n"), ge = m(s.FRAGMENT_SHADER, "\n    precision highp float;\n    precision highp sampler2D;\n    varying vec2 vUv;\n    uniform sampler2D uVelocity;\n    uniform sampler2D uSource;\n    uniform vec2 texelSize;\n    uniform vec2 dyeTexelSize;\n    uniform float dt;\n    uniform float dissipation;\n    vec4 bilerp (sampler2D sam, vec2 uv, vec2 tsize) {\n        vec2 st = uv / tsize - 0.5;\n        vec2 iuv = floor(st);\n        vec2 fuv = fract(st);\n        vec4 a = texture2D(sam, (iuv + vec2(0.5, 0.5)) * tsize);\n        vec4 b = texture2D(sam, (iuv + vec2(1.5, 0.5)) * tsize);\n        vec4 c = texture2D(sam, (iuv + vec2(0.5, 1.5)) * tsize);\n        vec4 d = texture2D(sam, (iuv + vec2(1.5, 1.5)) * tsize);\n        return mix(mix(a, b, fuv.x), mix(c, d, fuv.x), fuv.y);\n    }\n    void main () {\n    #ifdef MANUAL_FILTERING\n        vec2 coord = vUv - dt * bilerp(uVelocity, vUv, texelSize).xy * texelSize;\n        vec4 result = bilerp(uSource, coord, dyeTexelSize);\n    #else\n        vec2 coord = vUv - dt * texture2D(uVelocity, vUv).xy * texelSize;\n        vec4 result = texture2D(uSource, coord);\n    #endif\n        float decay = 1.0 + dissipation * dt;\n        gl_FragColor = result / decay;\n    }", c.supportLinearFiltering ? null : ["MANUAL_FILTERING"]), _e = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    precision mediump sampler2D;\n    varying highp vec2 vUv;\n    varying highp vec2 vL;\n    varying highp vec2 vR;\n    varying highp vec2 vT;\n    varying highp vec2 vB;\n    uniform sampler2D uVelocity;\n    void main () {\n        float L = texture2D(uVelocity, vL).x;\n        float R = texture2D(uVelocity, vR).x;\n        float T = texture2D(uVelocity, vT).y;\n        float B = texture2D(uVelocity, vB).y;\n        vec2 C = texture2D(uVelocity, vUv).xy;\n        if (vL.x < 0.0) { L = -C.x; }\n        if (vR.x > 1.0) { R = -C.x; }\n        if (vT.y > 1.0) { T = -C.y; }\n        if (vB.y < 0.0) { B = -C.y; }\n        float div = 0.5 * (R - L + T - B);\n        gl_FragColor = vec4(div, 0.0, 0.0, 1.0);\n    }\n"), ve = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    precision mediump sampler2D;\n    varying highp vec2 vUv;\n    varying highp vec2 vL;\n    varying highp vec2 vR;\n    varying highp vec2 vT;\n    varying highp vec2 vB;\n    uniform sampler2D uVelocity;\n    void main () {\n        float L = texture2D(uVelocity, vL).y;\n        float R = texture2D(uVelocity, vR).y;\n        float T = texture2D(uVelocity, vT).x;\n        float B = texture2D(uVelocity, vB).x;\n        float vorticity = R - L - T + B;\n        gl_FragColor = vec4(0.5 * vorticity, 0.0, 0.0, 1.0);\n    }\n"), ye = m(s.FRAGMENT_SHADER, "\n    precision highp float;\n    precision highp sampler2D;\n    varying vec2 vUv;\n    varying vec2 vL;\n    varying vec2 vR;\n    varying vec2 vT;\n    varying vec2 vB;\n    uniform sampler2D uVelocity;\n    uniform sampler2D uCurl;\n    uniform float curl;\n    uniform float dt;\n    void main () {\n        float L = texture2D(uCurl, vL).x;\n        float R = texture2D(uCurl, vR).x;\n        float T = texture2D(uCurl, vT).x;\n        float B = texture2D(uCurl, vB).x;\n        float C = texture2D(uCurl, vUv).x;\n        vec2 force = 0.5 * vec2(abs(T) - abs(B), abs(R) - abs(L));\n        force /= length(force) + 0.0001;\n        force *= curl * C;\n        force.y *= -1.0;\n        vec2 vel = texture2D(uVelocity, vUv).xy;\n        gl_FragColor = vec4(vel + force * dt, 0.0, 1.0);\n    }\n"), be = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    precision mediump sampler2D;\n    varying highp vec2 vUv;\n    varying highp vec2 vL;\n    varying highp vec2 vR;\n    varying highp vec2 vT;\n    varying highp vec2 vB;\n    uniform sampler2D uPressure;\n    uniform sampler2D uDivergence;\n    void main () {\n        float L = texture2D(uPressure, vL).x;\n        float R = texture2D(uPressure, vR).x;\n        float T = texture2D(uPressure, vT).x;\n        float B = texture2D(uPressure, vB).x;\n        float C = texture2D(uPressure, vUv).x;\n        float divergence = texture2D(uDivergence, vUv).x;\n        float pressure = (L + R + B + T - divergence) * 0.25;\n        gl_FragColor = vec4(pressure, 0.0, 0.0, 1.0);\n    }\n"), xe = m(s.FRAGMENT_SHADER, "\n    precision mediump float;\n    precision mediump sampler2D;\n    varying highp vec2 vUv;\n    varying highp vec2 vL;\n    varying highp vec2 vR;\n    varying highp vec2 vT;\n    varying highp vec2 vB;\n    uniform sampler2D uPressure;\n    uniform sampler2D uVelocity;\n    void main () {\n        float L = texture2D(uPressure, vL).x;\n        float R = texture2D(uPressure, vR).x;\n        float T = texture2D(uPressure, vT).x;\n        float B = texture2D(uPressure, vB).x;\n        vec2 velocity = texture2D(uVelocity, vUv).xy;\n        velocity.xy -= vec2(R - L, T - B);\n        gl_FragColor = vec4(velocity, 0.0, 1.0);\n    }\n"), g = (s.bindBuffer(s.ARRAY_BUFFER, s.createBuffer()), s.bufferData(s.ARRAY_BUFFER, new Float32Array([
    -1,
    -1,
    -1,
    1,
    1,
    1,
    1,
    -1
  ]), s.STATIC_DRAW), s.bindBuffer(s.ELEMENT_ARRAY_BUFFER, s.createBuffer()), s.bufferData(s.ELEMENT_ARRAY_BUFFER, new Uint16Array([
    0,
    1,
    2,
    0,
    2,
    3
  ]), s.STATIC_DRAW), s.vertexAttribPointer(0, 2, s.FLOAT, false, 0, 0), s.enableVertexAttribArray(0), (e3) => {
    s.bindFramebuffer(s.FRAMEBUFFER, e3), s.drawElements(s.TRIANGLES, 6, s.UNSIGNED_SHORT, 0);
  }), _, v, y, b, x, S, C, w, T = Te(), E = new d(ie, ae), D = new d(h, oe), O = new d(h, se), k = new d(h, ce), A = new d(h, le), j = new d(h, ue), M = new d(h, de), N = new d(h, fe), P = new d(h, pe), F = new d(h, me), I = new d(h, he), L = new d(h, ge), R = new d(h, _e), z = new d(h, ve), B = new d(h, ye), V = new d(h, be), H = new d(h, xe), U = new ne(h, "\n    precision highp float;\n    precision highp sampler2D;\n    varying vec2 vUv;\n    varying vec2 vL;\n    varying vec2 vR;\n    varying vec2 vT;\n    varying vec2 vB;\n    uniform sampler2D uTexture;\n    uniform sampler2D uBloom;\n    uniform sampler2D uSunrays;\n    uniform sampler2D uDithering;\n    uniform vec2 ditherScale;\n    uniform vec2 texelSize;\n    vec3 linearToGamma (vec3 color) {\n        color = max(color, vec3(0));\n        return max(1.055 * pow(color, vec3(0.416666667)) - 0.055, vec3(0));\n    }\n    void main () {\n        vec3 c = texture2D(uTexture, vUv).rgb;\n    #ifdef SHADING\n        vec3 lc = texture2D(uTexture, vL).rgb;\n        vec3 rc = texture2D(uTexture, vR).rgb;\n        vec3 tc = texture2D(uTexture, vT).rgb;\n        vec3 bc = texture2D(uTexture, vB).rgb;\n        float dx = length(rc) - length(lc);\n        float dy = length(tc) - length(bc);\n        vec3 n = normalize(vec3(dx, dy, length(texelSize)));\n        vec3 l = vec3(0.0, 0.0, 1.0);\n        float diffuse = clamp(dot(n, l) + 0.7, 0.7, 1.0);\n        c *= diffuse;\n    #endif\n    #ifdef BLOOM\n        vec3 bloom = texture2D(uBloom, vUv).rgb;\n    #endif\n    #ifdef SUNRAYS\n        float sunrays = texture2D(uSunrays, vUv).r;\n        c *= sunrays;\n    #ifdef BLOOM\n        bloom *= sunrays;\n    #endif\n    #endif\n    #ifdef BLOOM\n        float noise = texture2D(uDithering, vUv * ditherScale).r;\n        noise = noise * 2.0 - 1.0;\n        bloom += noise / 255.0;\n        bloom = linearToGamma(bloom);\n        c += bloom;\n    #endif\n        float a = max(c.r, max(c.g, c.b));\n        gl_FragColor = vec4(c, a);\n    }\n");
  function W() {
    let e3 = Q(t.SIM_RESOLUTION), n2 = Q(t.DYE_RESOLUTION), r2 = c.halfFloatTexType, i2 = c.formatRGBA, a2 = c.formatRG, o2 = c.formatR, l2 = c.supportLinearFiltering ? s.LINEAR : s.NEAREST;
    _ = _ ? q(_, n2.width, n2.height, i2.internalFormat, i2.format, r2, l2) : K(n2.width, n2.height, i2.internalFormat, i2.format, r2, l2), v = v ? q(v, e3.width, e3.height, a2.internalFormat, a2.format, r2, l2) : K(e3.width, e3.height, a2.internalFormat, a2.format, r2, l2), y = G(e3.width, e3.height, o2.internalFormat, o2.format, r2, s.NEAREST), b = G(e3.width, e3.height, o2.internalFormat, o2.format, r2, s.NEAREST), x = K(e3.width, e3.height, o2.internalFormat, o2.format, r2, s.NEAREST), Se(), Ce();
  }
  function Se() {
    let e3 = Q(t.BLOOM_RESOLUTION), n2 = c.halfFloatTexType, r2 = c.formatRGBA, i2 = c.supportLinearFiltering ? s.LINEAR : s.NEAREST;
    S = G(e3.width, e3.height, r2.internalFormat, r2.format, n2, i2), o.length = 0;
    for (let a2 = 0; a2 < t.BLOOM_ITERATIONS; a2++) {
      let t2 = e3.width >> a2 + 1, s2 = e3.height >> a2 + 1;
      if (t2 < 2 || s2 < 2) break;
      let c2 = G(t2, s2, r2.internalFormat, r2.format, n2, i2);
      o.push(c2);
    }
  }
  function Ce() {
    let e3 = Q(t.SUNRAYS_RESOLUTION), n2 = c.halfFloatTexType, r2 = c.formatR, i2 = c.supportLinearFiltering ? s.LINEAR : s.NEAREST;
    C = G(e3.width, e3.height, r2.internalFormat, r2.format, n2, i2), w = G(e3.width, e3.height, r2.internalFormat, r2.format, n2, i2);
  }
  function G(e3, t2, n2, r2, i2, a2) {
    s.activeTexture(s.TEXTURE0);
    let o2 = s.createTexture();
    s.bindTexture(s.TEXTURE_2D, o2), s.texParameteri(s.TEXTURE_2D, s.TEXTURE_MIN_FILTER, a2), s.texParameteri(s.TEXTURE_2D, s.TEXTURE_MAG_FILTER, a2), s.texParameteri(s.TEXTURE_2D, s.TEXTURE_WRAP_S, s.CLAMP_TO_EDGE), s.texParameteri(s.TEXTURE_2D, s.TEXTURE_WRAP_T, s.CLAMP_TO_EDGE), s.texImage2D(s.TEXTURE_2D, 0, n2, e3, t2, 0, r2, i2, null);
    let c2 = s.createFramebuffer();
    return s.bindFramebuffer(s.FRAMEBUFFER, c2), s.framebufferTexture2D(s.FRAMEBUFFER, s.COLOR_ATTACHMENT0, s.TEXTURE_2D, o2, 0), s.viewport(0, 0, e3, t2), s.clear(s.COLOR_BUFFER_BIT), {
      texture: o2,
      fbo: c2,
      width: e3,
      height: t2,
      texelSizeX: 1 / e3,
      texelSizeY: 1 / t2,
      attach(e4) {
        return s.activeTexture(s.TEXTURE0 + e4), s.bindTexture(s.TEXTURE_2D, o2), e4;
      }
    };
  }
  function K(e3, t2, n2, r2, i2, a2) {
    let o2 = G(e3, t2, n2, r2, i2, a2), s2 = G(e3, t2, n2, r2, i2, a2);
    return {
      width: e3,
      height: t2,
      texelSizeX: o2.texelSizeX,
      texelSizeY: o2.texelSizeY,
      get read() {
        return o2;
      },
      set read(e4) {
        o2 = e4;
      },
      get write() {
        return s2;
      },
      set write(e4) {
        s2 = e4;
      },
      swap() {
        let e4 = o2;
        o2 = s2, s2 = e4;
      }
    };
  }
  function we(e3, t2, n2, r2, i2, a2, o2) {
    let c2 = G(t2, n2, r2, i2, a2, o2);
    return D.bind(), s.uniform1i(D.uniforms.uTexture, e3.attach(0)), g(c2.fbo), c2;
  }
  function q(e3, t2, n2, r2, i2, a2, o2) {
    return e3.width === t2 && e3.height === n2 ? e3 : (e3.read = we(e3.read, t2, n2, r2, i2, a2, o2), e3.write = G(t2, n2, r2, i2, a2, o2), e3.width = t2, e3.height = n2, e3.texelSizeX = 1 / t2, e3.texelSizeY = 1 / n2, e3);
  }
  function Te(e3) {
    let t2 = s.createTexture();
    s.bindTexture(s.TEXTURE_2D, t2), s.texParameteri(s.TEXTURE_2D, s.TEXTURE_MIN_FILTER, s.LINEAR), s.texParameteri(s.TEXTURE_2D, s.TEXTURE_MAG_FILTER, s.LINEAR), s.texParameteri(s.TEXTURE_2D, s.TEXTURE_WRAP_S, s.REPEAT), s.texParameteri(s.TEXTURE_2D, s.TEXTURE_WRAP_T, s.REPEAT), s.texImage2D(s.TEXTURE_2D, 0, s.RGB, 1, 1, 0, s.RGB, s.UNSIGNED_BYTE, new Uint8Array([
      255,
      255,
      255
    ]));
    let n2 = {
      texture: t2,
      width: 1,
      height: 1,
      attach(e4) {
        return s.activeTexture(s.TEXTURE0 + e4), s.bindTexture(s.TEXTURE_2D, t2), e4;
      }
    }, r2 = new Image();
    return r2.onload = () => {
      n2.width = r2.width, n2.height = r2.height, s.bindTexture(s.TEXTURE_2D, t2), s.texImage2D(s.TEXTURE_2D, 0, s.RGB, s.RGB, s.UNSIGNED_BYTE, r2);
    }, e3 && (r2.src = e3), n2;
  }
  function Ee() {
    let e3 = [];
    t.SHADING && e3.push("SHADING"), t.BLOOM && e3.push("BLOOM"), t.SUNRAYS && e3.push("SUNRAYS"), U.setKeywords(e3);
  }
  Ee(), W(), t.IMMEDIATE && Ve(t.SPLAT_COUNT);
  function J() {
    t.AUTO && t.INTERVAL && !t.PAUSED && a.push(t.SPLAT_COUNT), setTimeout(J, t.INTERVAL);
  }
  setTimeout(J, t.INTERVAL);
  let Y = Date.now(), X = 0;
  De();
  function De() {
    let e3 = Oe();
    ke() && W(), Ae(e3), je(), t.PAUSED || Me(e3), Ne(null), requestAnimationFrame(De);
  }
  function Oe() {
    let e3 = Date.now(), t2 = (e3 - Y) / 1e3;
    return t2 = Math.min(t2, 0.016666), Y = e3, t2;
  }
  function ke() {
    let e3 = $(n.clientWidth), t2 = $(n.clientHeight);
    return n.width !== e3 || n.height !== t2 ? (n.width = e3, n.height = t2, true) : false;
  }
  function Ae(e3) {
    t.COLORFUL && (X += e3 * t.COLOR_UPDATE_SPEED, X >= 1 && (X = Qe(X, 0, 1), i.forEach((e4) => {
      e4.color = Z();
    })));
  }
  function je() {
    a.length > 0 && Ve(a.pop()), i.forEach((e3) => {
      e3.moved && (e3.moved = false, Be(e3));
    });
  }
  function Me(e3) {
    s.disable(s.BLEND), s.viewport(0, 0, v.width, v.height), z.bind(), s.uniform2f(z.uniforms.texelSize, v.texelSizeX, v.texelSizeY), s.uniform1i(z.uniforms.uVelocity, v.read.attach(0)), g(b.fbo), B.bind(), s.uniform2f(B.uniforms.texelSize, v.texelSizeX, v.texelSizeY), s.uniform1i(B.uniforms.uVelocity, v.read.attach(0)), s.uniform1i(B.uniforms.uCurl, b.attach(1)), s.uniform1f(B.uniforms.curl, t.CURL), s.uniform1f(B.uniforms.dt, e3), g(v.write.fbo), v.swap(), R.bind(), s.uniform2f(R.uniforms.texelSize, v.texelSizeX, v.texelSizeY), s.uniform1i(R.uniforms.uVelocity, v.read.attach(0)), g(y.fbo), O.bind(), s.uniform1i(O.uniforms.uTexture, x.read.attach(0)), s.uniform1f(O.uniforms.value, t.PRESSURE), g(x.write.fbo), x.swap(), V.bind(), s.uniform2f(V.uniforms.texelSize, v.texelSizeX, v.texelSizeY), s.uniform1i(V.uniforms.uDivergence, y.attach(0));
    for (let e4 = 0; e4 < t.PRESSURE_ITERATIONS; e4++) s.uniform1i(V.uniforms.uPressure, x.read.attach(1)), g(x.write.fbo), x.swap();
    H.bind(), s.uniform2f(H.uniforms.texelSize, v.texelSizeX, v.texelSizeY), s.uniform1i(H.uniforms.uPressure, x.read.attach(0)), s.uniform1i(H.uniforms.uVelocity, v.read.attach(1)), g(v.write.fbo), v.swap(), L.bind(), s.uniform2f(L.uniforms.texelSize, v.texelSizeX, v.texelSizeY), c.supportLinearFiltering || s.uniform2f(L.uniforms.dyeTexelSize, v.texelSizeX, v.texelSizeY);
    let n2 = v.read.attach(0);
    s.uniform1i(L.uniforms.uVelocity, n2), s.uniform1i(L.uniforms.uSource, n2), s.uniform1f(L.uniforms.dt, e3), s.uniform1f(L.uniforms.dissipation, t.VELOCITY_DISSIPATION), g(v.write.fbo), v.swap(), s.viewport(0, 0, _.width, _.height), c.supportLinearFiltering || s.uniform2f(L.uniforms.dyeTexelSize, _.texelSizeX, _.texelSizeY), s.uniform1i(L.uniforms.uVelocity, v.read.attach(0)), s.uniform1i(L.uniforms.uSource, _.read.attach(1)), s.uniform1f(L.uniforms.dissipation, t.DENSITY_DISSIPATION), g(_.write.fbo), _.swap();
  }
  function Ne(e3) {
    t.BLOOM && Le(_.read, S), t.SUNRAYS && (Re(_.read, _.write, C), ze(C, w, 1)), !e3 || !t.TRANSPARENT ? (s.blendFunc(s.ONE, s.ONE_MINUS_SRC_ALPHA), s.enable(s.BLEND)) : s.disable(s.BLEND);
    let n2 = e3 ? e3.width : s.drawingBufferWidth, r2 = e3 ? e3.height : s.drawingBufferHeight;
    s.viewport(0, 0, n2, r2);
    let i2 = e3 ? e3.fbo : null;
    t.TRANSPARENT || Pe(i2, Ze(t.BACK_COLOR)), !e3 && t.TRANSPARENT && Fe(i2), Ie(i2, n2, r2);
  }
  function Pe(e3, t2) {
    k.bind(), s.uniform4f(k.uniforms.color, t2.r, t2.g, t2.b, 1), g(e3);
  }
  function Fe(e3) {
    A.bind(), s.uniform1f(A.uniforms.aspectRatio, n.width / n.height), g(e3);
  }
  function Ie(e3, n2, r2) {
    if (U.bind(), t.SHADING && s.uniform2f(U.uniforms.texelSize, 1 / n2, 1 / r2), s.uniform1i(U.uniforms.uTexture, _.read.attach(0)), t.BLOOM) {
      s.uniform1i(U.uniforms.uBloom, S.attach(1)), s.uniform1i(U.uniforms.uDithering, T.attach(2));
      let e4 = $e(T, n2, r2);
      s.uniform2f(U.uniforms.ditherScale, e4.x, e4.y);
    }
    t.SUNRAYS && s.uniform1i(U.uniforms.uSunrays, C.attach(3)), g(e3);
  }
  function Le(e3, n2) {
    if (o.length < 2) return;
    let r2 = n2;
    s.disable(s.BLEND), j.bind();
    let i2 = t.BLOOM_THRESHOLD * t.BLOOM_SOFT_KNEE + 1e-4, a2 = t.BLOOM_THRESHOLD - i2, c2 = i2 * 2, l2 = 0.25 / i2;
    s.uniform3f(j.uniforms.curve, a2, c2, l2), s.uniform1f(j.uniforms.threshold, t.BLOOM_THRESHOLD), s.uniform1i(j.uniforms.uTexture, e3.attach(0)), s.viewport(0, 0, r2.width, r2.height), g(r2.fbo), M.bind();
    for (let e4 = 0; e4 < o.length; e4++) {
      let t2 = o[e4];
      s.uniform2f(M.uniforms.texelSize, r2.texelSizeX, r2.texelSizeY), s.uniform1i(M.uniforms.uTexture, r2.attach(0)), s.viewport(0, 0, t2.width, t2.height), g(t2.fbo), r2 = t2;
    }
    s.blendFunc(s.ONE, s.ONE), s.enable(s.BLEND);
    for (let e4 = o.length - 2; e4 >= 0; e4--) {
      let t2 = o[e4];
      s.uniform2f(M.uniforms.texelSize, r2.texelSizeX, r2.texelSizeY), s.uniform1i(M.uniforms.uTexture, r2.attach(0)), s.viewport(0, 0, t2.width, t2.height), g(t2.fbo), r2 = t2;
    }
    s.disable(s.BLEND), N.bind(), s.uniform2f(N.uniforms.texelSize, r2.texelSizeX, r2.texelSizeY), s.uniform1i(N.uniforms.uTexture, r2.attach(0)), s.uniform1f(N.uniforms.intensity, t.BLOOM_INTENSITY), s.viewport(0, 0, n2.width, n2.height), g(n2.fbo);
  }
  function Re(e3, n2, r2) {
    s.disable(s.BLEND), P.bind(), s.uniform1i(P.uniforms.uTexture, e3.attach(0)), s.viewport(0, 0, n2.width, n2.height), g(n2.fbo), F.bind(), s.uniform1f(F.uniforms.weight, t.SUNRAYS_WEIGHT), s.uniform1i(F.uniforms.uTexture, n2.attach(0)), s.viewport(0, 0, r2.width, r2.height), g(r2.fbo);
  }
  function ze(e3, t2, n2) {
    E.bind();
    for (let r2 = 0; r2 < n2; r2++) s.uniform2f(E.uniforms.texelSize, e3.texelSizeX, 0), s.uniform1i(E.uniforms.uTexture, e3.attach(0)), g(t2.fbo), s.uniform2f(E.uniforms.texelSize, 0, e3.texelSizeY), s.uniform1i(E.uniforms.uTexture, t2.attach(0)), g(e3.fbo);
  }
  function Be(e3) {
    let n2 = e3.deltaX * t.SPLAT_FORCE, r2 = e3.deltaY * t.SPLAT_FORCE;
    He(e3.texcoordX, e3.texcoordY, n2, r2, e3.color);
  }
  function Ve(e3) {
    for (let n2 = 0; n2 < e3; n2++) {
      let e4 = Z();
      t.SPLAT_COLOR || (e4.r *= 10, e4.g *= 10, e4.b *= 10), He(Math.random(), Math.random(), 1e3 * (Math.random() - 0.5), 1e3 * (Math.random() - 0.5), e4);
    }
  }
  function He(e3, r2, i2, a2, o2) {
    s.viewport(0, 0, v.width, v.height), I.bind(), s.uniform1i(I.uniforms.uTarget, v.read.attach(0)), s.uniform1f(I.uniforms.aspectRatio, n.width / n.height), s.uniform2f(I.uniforms.point, e3, r2), s.uniform3f(I.uniforms.color, i2, a2, 0), s.uniform1f(I.uniforms.radius, Ue(t.SPLAT_RADIUS / 100)), g(v.write.fbo), v.swap(), s.viewport(0, 0, _.width, _.height), s.uniform1i(I.uniforms.uTarget, _.read.attach(0)), s.uniform3f(I.uniforms.color, o2.r, o2.g, o2.b), g(_.write.fbo), _.swap();
  }
  function Ue(e3) {
    let t2 = n.width / n.height;
    return t2 > 1 && (e3 *= t2), e3;
  }
  n.addEventListener("mousedown", (e3) => {
    let t2 = $(e3.offsetX), n2 = $(e3.offsetY), a2 = i.find((e4) => e4.id === -1);
    a2 || (a2 = new r()), We(a2, -1, t2, n2);
  }), setTimeout(() => {
    n.addEventListener("mousemove", (e3) => {
      let t2 = $(e3.offsetX), n2 = $(e3.offsetY);
      Ge(i[0], t2, n2);
    });
  }, 500), window.addEventListener("mouseup", () => {
    Ke(i[0]);
  }), n.addEventListener("touchstart", (e3) => {
    e3.preventDefault();
    let t2 = e3.targetTouches;
    for (; t2.length >= i.length; ) i.push(new r());
    for (let e4 = 0; e4 < t2.length; e4++) {
      let n2 = $(t2[e4].pageX), r2 = $(t2[e4].pageY);
      We(i[e4 + 1], t2[e4].identifier, n2, r2);
    }
  }), n.addEventListener("touchmove", (e3) => {
    e3.preventDefault();
    let t2 = e3.targetTouches;
    for (let e4 = 0; e4 < t2.length; e4++) {
      let n2 = $(t2[e4].pageX), r2 = $(t2[e4].pageY);
      Ge(i[e4 + 1], n2, r2);
    }
  }, false), window.addEventListener("touchend", (e3) => {
    let t2 = e3.changedTouches;
    for (let e4 = 0; e4 < t2.length; e4++) Ke(i.find((n2) => n2.id === t2[e4].identifier));
  }), window.addEventListener("keydown", (e3) => {
    e3.code === "KeyP" && (t.PAUSED = !t.PAUSED), e3.key === " " && a.push(Number.parseInt(Math.random() * 20) + 5);
  });
  function We(e3, r2, i2, a2) {
    e3.id = r2, e3.down = true, e3.moved = false, e3.texcoordX = i2 / n.width, e3.texcoordY = 1 - a2 / n.height, e3.prevTexcoordX = e3.texcoordX, e3.prevTexcoordY = e3.texcoordY, e3.deltaX = 0, e3.deltaY = 0, t.COLORFUL && (e3.color = Z());
  }
  function Ge(e3, r2, i2) {
    t.TRIGGER === "click" && (e3.moved = e3.down), e3.prevTexcoordX = e3.texcoordX, e3.prevTexcoordY = e3.texcoordY, e3.texcoordX = r2 / n.width, e3.texcoordY = 1 - i2 / n.height, e3.deltaX = qe(e3.texcoordX - e3.prevTexcoordX), e3.deltaY = Je(e3.texcoordY - e3.prevTexcoordY), t.TRIGGER === "hover" && (e3.moved = Math.abs(e3.deltaX) > 0 || Math.abs(e3.deltaY) > 0);
  }
  function Ke(e3) {
    e3.down = false;
  }
  function qe(e3) {
    let t2 = n.width / n.height;
    return t2 < 1 && (e3 *= t2), e3;
  }
  function Je(e3) {
    let t2 = n.width / n.height;
    return t2 > 1 && (e3 /= t2), e3;
  }
  function Ye() {
    let e3 = Xe(Math.random(), 1, 1);
    return e3.r *= 0.15, e3.g *= 0.15, e3.b *= 0.15, e3;
  }
  function Z() {
    let e3 = t.SPLAT_COLOR || Ye();
    return {
      r: e3.r,
      g: e3.g,
      b: e3.b
    };
  }
  function Xe(e3, t2, n2) {
    let r2, i2, a2, o2 = Math.floor(e3 * 6), s2 = e3 * 6 - o2, c2 = n2 * (1 - t2), l2 = n2 * (1 - s2 * t2), u2 = n2 * (1 - (1 - s2) * t2);
    switch (o2 % 6) {
      case 0:
        r2 = n2, i2 = u2, a2 = c2;
        break;
      case 1:
        r2 = l2, i2 = n2, a2 = c2;
        break;
      case 2:
        r2 = c2, i2 = n2, a2 = u2;
        break;
      case 3:
        r2 = c2, i2 = l2, a2 = n2;
        break;
      case 4:
        r2 = u2, i2 = c2, a2 = n2;
        break;
      case 5:
        r2 = n2, i2 = c2, a2 = l2;
        break;
    }
    return {
      r: r2,
      g: i2,
      b: a2
    };
  }
  function Ze(e3) {
    return {
      r: e3.r / 255,
      g: e3.g / 255,
      b: e3.b / 255
    };
  }
  function Qe(e3, t2, n2) {
    let r2 = n2 - t2;
    return r2 === 0 ? t2 : (e3 - t2) % r2 + t2;
  }
  function Q(e3) {
    let t2 = s.drawingBufferWidth / s.drawingBufferHeight;
    t2 < 1 && (t2 = 1 / t2);
    let n2 = Math.round(e3), r2 = Math.round(e3 * t2);
    return s.drawingBufferWidth > s.drawingBufferHeight ? {
      width: r2,
      height: n2
    } : {
      width: n2,
      height: r2
    };
  }
  function $e(e3, t2, n2) {
    return {
      x: t2 / e3.width,
      y: n2 / e3.height
    };
  }
  function $(e3) {
    let t2 = window.devicePixelRatio || 1;
    return Math.floor(e3 * t2);
  }
  function et(e3) {
    if (e3.length === 0) return 0;
    let t2 = 0;
    for (let n2 = 0; n2 < e3.length; n2++) t2 = (t2 << 5) - t2 + e3.charCodeAt(n2), t2 |= 0;
    return t2;
  }
}
export {
  e as default
};
//# sourceMappingURL=webgl-fluid.js.map
